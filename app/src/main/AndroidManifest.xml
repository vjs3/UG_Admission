<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vishwajeets3.loginregistration"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/LiveoActionBarTheme" >
        <activity
            android:name=".activity.LoginChoiceActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activity.RegisterActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" />
        <activity
            android:name=".navigationliveo.NavigationMain"
            android:label="UG Admissions" />

        <activity
            android:name=".activity.HomeScreenAdminActivity"
            android:label="Administration" >
        </activity>

        <activity
            android:name=".activity.RoundStatusActivity"
            android:label="Counselling Status">
        </activity>

        <activity
            android:name=".activity.SetDatesActivity"
            android:label="Conduct Round">
        </activity>

        <activity
            android:name=".activity.StudentStatusActivity"
            android:label="Student Status">
        </activity>
    </application>

</manifest>
